@extends('layouts.app')

@section('content')
<div class="container">
    <div class="searchbar mt-0 mb-4">
        <div class="row">
            <div class="col-md-6">
                <form>
                    <div class="input-group">
                        <input
                            id="indexSearch"
                            type="text"
                            name="search"
                            placeholder="{{ __('crud.common.search') }}"
                            value="{{ $search ?? '' }}"
                            class="form-control"
                            autocomplete="off"
                        />
                        <div class="input-group-append">
                            <button type="submit" class="btn btn-primary">
                                <i class="icon ion-md-search"></i>
                            </button>
                        </div>
                    </div>
                </form>
            </div>
            <div class="col-md-6 text-right">
                @can('create', App\Models\Emploi::class)
                <a href="{{ route('emplois.create') }}" class="btn btn-primary">
                    <i class="icon ion-md-add"></i> @lang('crud.common.create')
                </a>
                @endcan
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div style="display: flex; justify-content: space-between;">
                <h4 class="card-title">
                    @lang('crud.all_emploitemps.index_title')
                </h4>
            </div>

            <div class="table-responsive">
                <table class="table table-borderless table-hover">
                    <thead>
                        <tr>
                            <th class="text-left">
                                @lang('crud.all_emploitemps.inputs.classe_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_emploitemps.inputs.salle_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_emploitemps.inputs.user_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_emploitemps.inputs.Ddebut')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_emploitemps.inputs.Dfin')
                            </th>
                            <th class="text-left">
                                @lang('crud.all_emploitemps.inputs.prof_id')
                            </th>
                            <th class="text-center">
                                @lang('crud.common.actions')
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse($emplois as $emploi)
                        <tr>
                            <td>{{ optional($emploi->classe)->nom ?? '-' }}</td>
                            <td>{{ optional($emploi->salle)->nom ?? '-' }}</td>
                            <td>{{ optional($emploi->user)->name ?? '-' }}</td>
                            <td>{{ $emploi->Ddebut ?? '-' }}</td>
                            <td>{{ $emploi->Dfin ?? '-' }}</td>
                            <td>{{ optional($emploi->prof)->nom ?? '-' }}</td>
                            <td class="text-center" style="width: 134px;">
                                <div
                                    role="group"
                                    aria-label="Row Actions"
                                    class="btn-group"
                                >
                                    @can('update', $emploi)
                                    <a
                                        href="{{ route('emplois.edit', $emploi) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-create"></i>
                                        </button>
                                    </a>
                                    @endcan @can('view', $emploi)
                                    <a
                                        href="{{ route('emplois.show', $emploi) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-eye"></i>
                                        </button>
                                    </a>
                                    @endcan @can('delete', $emploi)
                                    <form
                                        action="{{ route('emplois.destroy', $emploi) }}"
                                        method="POST"
                                        onsubmit="return confirm('{{ __('crud.common.are_you_sure') }}')"
                                    >
                                        @csrf @method('DELETE')
                                        <button
                                            type="submit"
                                            class="btn btn-light text-danger"
                                        >
                                            <i class="icon ion-md-trash"></i>
                                        </button>
                                    </form>
                                    @endcan
                                </div>
                            </td>
                        </tr>
                        @empty
                        <tr>
                            <td colspan="7">
                                @lang('crud.common.no_items_found')
                            </td>
                        </tr>
                        @endforelse
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="7">{!! $emplois->render() !!}</td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
@endsection
